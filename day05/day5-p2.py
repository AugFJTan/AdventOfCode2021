# Consider only horizontal, vertical and diagonal lines
# Determine the number of points where at least two lines overlap (count > 2)

from vents import *

print(get_overlap(diagonal=True))  # Answer: 18423
